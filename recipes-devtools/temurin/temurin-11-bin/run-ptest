#!/bin/sh
set -euxo pipefail

FILE=/usr/lib/jvm/java-11-temurin/bin/java
if [ -f "$FILE" ]; then
    echo "$FILE exists. Using alternate java name to test."
    $FILE --version
else
    echo "$FILE does not exist. Using default java to test."
    java --version
fi

RETVAL=$?
if [ $RETVAL -eq 0 ] ; then
    echo "PASS: temurin-11-bin startup test"
else
    echo "FAIL: temurin-11-bin startup test"
fi